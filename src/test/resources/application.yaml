spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ingredients_test_db
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  mvc:
    hiddenmethod:
      filter:
        enabled: true